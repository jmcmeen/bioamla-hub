# Scripts

Various scripts used in the maintenance and development of this repo have been included here. 

Some of them require ffmpeg. On Ubuntu run the following commands.

    sudo apt update && sudo apt upgrade
    sudo apt install ffmpeg
    ffmpeg -version

